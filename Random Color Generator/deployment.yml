
apiVersion: apps/v1
kind: Deployment
metadata:
  name: random-color-generator-deployment
spec:
  replicas: 2  # Number of desired replicas
  selector:
    matchLabels:
      app: random-color-generator
  template:
    metadata:
      labels:
        app: random-color-generator
    spec:
      containers:
      - name: random-color-generator
        image: rameshkumarverma/random-color-generator
        ports:
        - containerPort: 80
